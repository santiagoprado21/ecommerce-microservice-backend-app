pipeline {
    agent any
    environment {
       IMAGE_NAME = "santiagoprado21/santiagoprado21-user-service-container"
       IMAGE_TAG = "0.0.1"
       DOCKER_CREDENTIALS_ID = "dockerhub-credentials"
    }

    stages {
        stage('Verify Java Version') {
            steps {
                script {
                    // Ejecuta el comando java -version para ver qué versión de Java se está utilizando
                    sh 'java -version'
                }
            }
        }
        stage('Checkout') {
            steps {
                git 'https://github.com/santiagoprado21/ecommerce-microservice-backend-app.git'
            }
        }
        stage('Build Docker Image') {
            steps {
                dir('user-service') {
                    script {
                        docker.build("${IMAGE_NAME}:${IMAGE_TAG}")
                    }
                }
            }
        }
        stage('Push Docker Image') {
            steps {
                script {
                    docker.withRegistry('https://index.docker.io/v1/', "${DOCKER_CREDENTIALS_ID}") {
                        docker.image("${IMAGE_NAME}:${IMAGE_TAG}").push()
                    }
                }
            }
        }
    }

}